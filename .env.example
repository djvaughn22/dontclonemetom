# Adoptable pet search (RescueGroups.org — free, nonprofit).
# Create a free key: https://rescuegroups.org/services/adoptable-pet-data-api/
# Then set this in Vercel (Project → Settings → Environment Variables). Never commit a real key.
RESCUEGROUPS_API_KEY=your_rescuegroups_api_key_here
